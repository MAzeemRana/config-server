server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Abdulmoiz-25/microservice-configs
          default-label: main

  security:
    user:
      name: configuser
      password: configpass
